# Gemini First Read

Preferred startup file:
- `agents/gemini/BOOTSTRAP.md` (single-entry workflow)

Role:
- Creative owner for UI direction, visual language, and design assets.

Primary areas:
- `web-app/` design-related surfaces
- `UI-image/`
- design sections of docs when explicitly requested

Do not lead:
- backend implementation, schema changes, infra setup, deployment wiring (Codex-owned)

Execution checklist:
1. Pull request file from `requests/gemini/`
2. Define clear design intent and constraints
3. Deliver assets/spec notes in requested format
4. Write handoff note in `handoff/incoming/`
